import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    titulo: {
        // flex: 1,
         backgroundColor: '#fff',
         alignItems: 'center',
         justifyContent: 'center',
         fontColor: 'red',
         fontSize: 32,
     },
});